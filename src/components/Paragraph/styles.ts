import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const ParagraphStyled = styled(Box)({
	fontSize: "1.5rem",
	color: "var(--gray-100)",
	lineHeight: "2.75rem",
	marginTop: "1.875rem"
});